function obtenerDatosEditar(id, Categoria) {

  // Repueblo el Formulario del Modal
  let idCategoria = $('#idCategoria').val(id);
  let editarCategoria = $('#editarCategoria').val(Categoria);
  //caturo el valor de los inputs
  var id = idCategoria.val();
  var categoria = editarCategoria.val();
}
